spring:
  application:
   name: ai-code-helper
  profiles:
    active: local
server:
  port: 8081
  servlet:
    context-path: /api
langchain4j:
  community:
    dashscope:
      chat-model:
        model-name: qwen3.8-max-0902
        api-key: < You API Key Here >
      embedding-model:
        model-name: qwen3.7-text-embedding
        api-key: < You API Key Here >
      streaming-chat-model:
        model-name: qwen3.8-max-0902
        api-key: < You API Key Here >
bigmodel:
  api-key: < You API Key Here >